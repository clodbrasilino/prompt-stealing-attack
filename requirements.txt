torch
torchvision
# inplace-abn replaced by pure-PyTorch compat module (inplace_abn.py)
# Eliminates CUDA extension build (GCC 13 + CUDA 13 header incompatibility)
pycocotools
randaugment
scikit-learn
fairscale
imagehash
ipyplot
numpy>=1.24
pandas>=2.0
scikit-image

setuptools

# Install CLIP via PyPI (git+https://github.com/openai/CLIP.git also works outside GFW)
openai-clip

# For finetune BLIP
timm==0.4.12
transformers>=4.15.0,<5.0  # or >=5.0 with med.py patch (see src/BLIP_finetune/models/med.py)
fairscale>=0.4.4
pycocoevalcap
opencv-python-headless
opencv-contrib-python-headless
ruamel.yaml>=0.17.32  # API fixed in src/models/subject_generator.py
datasets
pillow>=10.0
